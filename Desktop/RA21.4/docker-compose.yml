version: '3.8'
services:
  db:
    image: mongo
    ports:
      - '27017:27017'
  
  backend:
    build: ./server
    ports:
      - '9000:9000'
    depends_on:
      - db

  frontend:
    build: ./client
    ports:
      - '3000:3000'
    depends_on:
      - backend